# in this approach we use sliding window approach in which 
# we compare the semantic/cosine similarity between two embeddings and if the 
# two are very low that point indicates that point has changed...That means we can split there.

